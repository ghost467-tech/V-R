{
  "name": "voir-backend",
  "version": "0.1.0",
  "description": "Servidor de relay do VØĮR — diretório de chaves públicas + caixa de envelopes cifrados. Nunca vê texto em claro.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
